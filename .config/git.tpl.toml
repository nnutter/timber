# git-tpl — https://noirbizarre.github.io/git-tpl/configuration/
#
# This file is versioned with the project. Edit an answer and run
# `git tpl update` to re-render the template with it.

[template]
source = "git@github.com:nnutter/go-template"

[answers]
copyright_holder = "Nathaniel Nutter"
copyright_year = "2026"
formula_name = "timber"
go_version = "1.27.0"
module_path = "github.com/nnutter/timber"
project_name = "timber"
tap_owner = "nnutter"
tap_repository = "homebrew-tap"
with_homebrew = true
